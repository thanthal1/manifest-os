# The Manifest OS package repository — append to /etc/pacman.conf.
# Every package is signature-checked against the key that
# manifest-os-keyring installs into pacman's keyring.
[manifest-os]
SigLevel = Required DatabaseOptional
Server = https://github.com/thanthal1/manifest-os/releases/download/repo
